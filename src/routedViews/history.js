'use strict';

import createHistory from 'history/createBrowserHistory';
// may need to use this import for react native
// import createMemory from 'history/createMemoryHistory';


const history = createHistory();

export default history;